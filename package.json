{
  "name": "mind-the-moment",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Facts about where you're standing, right now. A proof-of-concept antidote to doom scrolling.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
